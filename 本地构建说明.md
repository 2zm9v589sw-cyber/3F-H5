# 本地构建说明

本项目使用 Next.js。

如果需要本地检查构建：

```bash
PATH=/Users/yikuaizhuantou/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH \
/Users/yikuaizhuantou/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm build
```

本次已完成生产构建检查，构建通过。
